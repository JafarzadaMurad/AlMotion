{
  "name": "almotion-ai-sidecar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OpenAI-compatible façade over CLI-harness AI backends (Claude Code subscription today; Gemini CLI and Codex later)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test src/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  }
}
